<% provide(:title, @student.name) %>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @student %>
        <%= @student.name %>
        <br>
        <br>
        <br>
        <br>
        <br>
        <br>
        <%= "Email: " + @student.email.to_s %>
        <br><br>
        <%= "Phone Number: " + @student.phone_number.to_s %>
        <br><br>
        <%= "Date of Birth: " + @student.dob.to_s %>
        <br><br>
        <%= "Education Level: " + @student.education_level.to_s %>
      </h1>
    </section>
  </aside>
</div>